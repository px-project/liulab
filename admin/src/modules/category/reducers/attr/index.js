/**
 * category attr reducer.
 */
import { CATEGORY_ADD_ATTR, CATEGORY_DELETE_ATTR, CATEGORY_EDIT_ATTR, CATEGORY_SAVE_ATTR } from '../../constants';

export default (state = {}, actions) => {

    if (actions.type === CATEGORY_ADD_ATTR) {

    }

    return state;
}