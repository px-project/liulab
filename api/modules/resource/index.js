/**
 * 资源路由
 */
const express = require('express');
const _router = express.Router();

module.exports = _router
    // 获取资源
    .get('/', (req, res) => {

    })

    // 创建资源
    .post('/', (req, res) => {

    })